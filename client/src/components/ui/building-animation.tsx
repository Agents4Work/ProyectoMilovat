"use client"

import React from "react"

export const BuildingAnimation: React.FC = () => {
  return (
    <div className="relative w-full h-full flex justify-center items-center">
      {/* Contenido del SVG eliminado según indicación */}
    </div>
  );
};